[pytest]
pythonpath = .
testpaths = tests
markers =
    regression: compares backend predictions with the notebook outputs (needs the challenge data)
    slow: long-running checks (all 68 Rail recordings)
