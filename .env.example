# Inlined at build time. Set the same keys in the Vercel project Environment Variables.
# Backend URL. Leave empty when the FastAPI backend serves this web build itself (same origin).
EXPO_PUBLIC_API_URL=http://127.0.0.1:8000
# true = bundled published predictions, false = live backend
EXPO_PUBLIC_USE_MOCK_API=false
